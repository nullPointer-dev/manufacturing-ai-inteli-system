"""
Shared constants used across the manufacturing AI backend.
Import from here — never hardcode these values inline.
"""

# kg CO₂ emitted per kWh of electricity consumed (average grid factor)
CO2_FACTOR: float = 0.82
